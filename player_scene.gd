extends Node2D

#var player_coins = 0
#
#func _ready():
	#var ui_node = get_node("UI")
	#if ui_node:
		#ui_node.update_coin_label()
#
#func add_coins(amount):
	#player_coins += amount
	#var ui_node = get_node("UI")
	#if ui_node:
		#ui_node.add_coins(amount)
	#print("Coins: " + str(player_coins))
